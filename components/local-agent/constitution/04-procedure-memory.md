# 04 - Procedure Memory

None yet. Mirrors the shape of `meta/procedural-memory`: record a mistake
lite Arc actually makes, and the rule that prevents repeating it, once
one happens. Empty is the honest starting state, not an omission.

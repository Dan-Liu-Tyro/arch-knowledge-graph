# 03 - Skills

None yet. The real Arc's skill index matches enabled workflows to tasks;
Arc Lite has no skills defined for the MVP — it follows
`01-working-protocol.md` directly for every question. Add a skill here
only once a repeated task pattern actually justifies one, per
`least-infrastructure-first` in `meta/architecture-learning`.
